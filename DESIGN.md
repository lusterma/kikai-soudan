# Kikai Soudan Design Guide

この文書は、Kikai Soudan のページ、フォーム、部品を追加・変更するときの
デザインルールです。

## デザイン目標

工場や保守の現場で、信頼でき、実用的で、迷わず使えるサイトを目指します。
装飾よりも、読みやすさと操作の分かりやすさを優先します。

- 専門的だが近寄りやすい
- 落ち着いた工業的な見た目
- スマートフォンでも読みやすい
- 操作と入力方法が明確
- 明るい現場や急いでいる状況でも使いやすい

## 色

| 用途 | CSS変数 | 色 |
| --- | --- | --- |
| 本文 | `--ink` | `#172027` |
| 補足文 | `--muted` | `#586673` |
| 境界線 | `--line` | `#D8E0E5` |
| ページ背景 | `--paper` | `#F4F7F8` |
| カード背景 | `--white` | `#FFFFFF` |
| メインカラー | `--steel` | `#1F4E5F` |
| 濃いメインカラー | `--steel-dark` | `#113642` |
| 主要アクション | `--accent` | `#D58A2D` |
| 補助アクション | `--green` | `#4F6540` |
| 薄い強調背景 | `--soft` | `#E8F0F3` |

オレンジは重要なボタンに使い、広い装飾面には多用しません。

## 文字

読み込みが速く、日本語を自然に表示できるシステムフォントを使います。

```css
font-family: "Yu Gothic", "Hiragino Kaku Gothic ProN", Meiryo, system-ui, sans-serif;
```

- 見出しは短く、太く、直接的にする
- 本文の行間は `1.7` から `1.9` 程度にする
- 本文は可能な限り `16px` 以上にする
- 補助ラベルも `12px` 未満にしない
- 長い文章を中央揃えにしない

## レイアウト

- コンテンツ最大幅は約 `1120px`
- スマートフォンの左右余白は最低 `18px`
- ラベル、入力欄、説明文をひとまとまりにする
- 大きなセクション間には十分な余白を置く
- 小さな画面ではフォームを1列にする
- 重要な情報をホバーだけで表示しない

## 部品

### ボタン

- 高さは最低 `44px`
- 「相談内容を入力する」など、動作が分かる文言にする
- 主要ボタンはオレンジを使う
- 補助ボタンは枠線、スチール色、または緑を使う
- ホバーとキーボードフォーカスを見えるようにする
- 同じ強さの主要ボタンを並べすぎない

### フォーム

- ラベルは入力欄の上に置く
- 必須・任意を文字で明記する
- 型式、症状、発生日などには入力例を示す
- エラーは該当する入力欄の近くに表示する
- プレースホルダーだけをラベル代わりにしない
- アップロード可能な形式と容量を明記する

### カード

- 白い背景、薄い境界線、控えめな影を使う
- 角丸は `6px` から `10px`
- 1枚につき1つの話題または操作を扱う
- カードを何重にも入れ子にしない

### 写真

- 実際の工作機械、保守、工場の写真を優先する
- 写真上の文字には暗いオーバーレイを敷く
- 内容が分かる代替テキストを付ける
- 一般的なオフィス写真や抽象的なIT画像は避ける

## 文章

利用者が急いでいたり、専門用語を知らなかったりすることを前提に、
平易で丁寧な日本語を使います。

- 1回に1つの具体的な情報を尋ねる
- 役立つ回答例を示す
- 必要な専門用語には説明を加える
- 確定診断と誤解される表現を避ける
- 観察結果、考えられる原因、次の行動を区別する
- 安全上の注意は直接的に目立たせる

## レスポンシブ対応

最低限、次の画面幅で確認します。

- 小型スマートフォン: `360px`
- 大型スマートフォン: `430px`
- タブレット: `768px`
- デスクトップ: `1280px`

ナビゲーション、ボタン、フォーム、ファイル選択が横スクロールなしで
操作できるようにします。

## アクセシビリティ

- 意味に合ったHTML要素を使う
- 可能な限り WCAG AA の色コントラストを満たす
- キーボードフォーカスを明確に表示する
- すべての入力欄にラベルを関連付ける
- 色だけで状態を伝えない
- `prefers-reduced-motion` に対応する
- 「こちら」ではなく、内容が分かるリンク文言を使う
- タップ領域は最低 `44px` 四方にする

## 動き

- アニメーションは操作結果を伝える目的に限定する
- 切り替え時間は通常 `120ms` から `200ms`
- フォーム付近で自動再生する動きを使わない
- パララックスや大きな登場アニメーションを避ける
- 動きを減らす設定では不要なアニメーションを止める

## 実装前チェック

- 色と文字のルールに沿っている
- `360px` 幅でもレイアウトが崩れない
- ボタンと入力欄をキーボードで操作できる
- フォーカス位置が見える
- 写真上の文字が読める
- フォームのエラーが理解できる
- 重要な情報がホバーや色だけに依存していない
- 日本語が簡潔で実用的である
